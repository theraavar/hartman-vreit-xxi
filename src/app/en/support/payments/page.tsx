import Vessel from "@/components/common/Vessel"
import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Tenants'
}

const Page = () => {
    return (
        <Vessel>
        </Vessel>
    )
}
export default Page