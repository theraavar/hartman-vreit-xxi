import Vessel from "@/components/common/Vessel"
import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Executives'
}

const Page = () => {
    return (
        <Vessel>
        </Vessel>
    )
}
export default Page